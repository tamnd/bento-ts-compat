bin/
.cache/
scratch/
*.out

# The accept tier's build-check and the runtime tier's go run each write to a
# scratch dir under the module tree and remove it, but a crashed run can leave one
# behind.
suite/buildcheck-*/
suite/runtime-*/
